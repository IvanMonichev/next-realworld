const config = {
  siteName: 'Next.js Real World',
  siteDescription: 'Next.js сodebase containing realworld examples (CRUD, auth, advanced patterns, etc) that adheres to the realworld spec and API'
}

export default config
